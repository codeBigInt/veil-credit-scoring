{
  "name": "contract",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test:compile": "compact compile --skip-zk src/main.compact ./src/managed/veil-protocol",
    "test": "vitest",
    "test:run": "vitest run",
    "compile": "compact compile src/main.compact ./src/managed/veil-protocol",
    "build": "rm -rf dist && tsc --project tsconfig.build.json && cp -Rf ./src/managed ./dist && cp ./src/main.compact ./dist && cp -Rf ./src/modules ./dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^4.0.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "^0.15.0"
  }
}
